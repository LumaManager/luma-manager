# Auth Real — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir auth mockado por auth real com banco, senhas hasheadas, TOTP real, sessão segura e onboarding interativo — deixando o sistema pronto para receber terapeutas reais.

**Architecture:** Drizzle ORM sobre Supabase Postgres (já configurado no projeto). Auth gerenciado pela própria API NestJS (não pelo Supabase Auth). TOTP real com `@otplib/core`. Onboarding como máquina de estado persistida no banco.

**Tech Stack:** NestJS + Drizzle ORM + postgres.js + jose (JWT já instalado) + @otplib/core (instalar) + bcryptjs (instalar)

---

## Visão geral dos 4 planos

Este é o **Plano 1 de 4**. A sequência completa:

| Plano | Escopo | Depende de |
|-------|--------|-----------|
| **1 — Auth Real** (este) | Schema base, auth, MFA, onboarding | — |
| 2 — Módulos Core | Patients, Appointments, Clinical Records | Plano 1 |
| 3 — Módulos Secundários | Finance, Documents, Dashboard real | Plano 2 |
| 4 — Produção | CI, cookies hardened, LGPD, observabilidade | Plano 3 |

---

## Mapa de arquivos

### Criar
| Arquivo | Responsabilidade |
|---------|-----------------|
| `apps/api/src/db/schema.ts` | Substituir schema atual com todas as tabelas do domínio auth |
| `apps/api/drizzle/0001_auth_base.sql` | Migration gerada pelo drizzle-kit |
| `apps/api/src/modules/auth/therapist.repository.ts` | Queries de therapist/auth no banco |
| `apps/api/src/modules/auth/mfa.service.ts` | TOTP: gerar segredo, verificar código, recovery codes |
| `apps/api/src/modules/auth/password.service.ts` | bcrypt: hash + verify |
| `apps/api/src/modules/onboarding/onboarding.service.ts` | Máquina de estado do onboarding |
| `apps/api/src/modules/onboarding/onboarding.controller.ts` | Endpoints POST /onboarding/... |
| `apps/api/src/modules/onboarding/onboarding.module.ts` | NestJS module |
| `apps/api/src/modules/onboarding/onboarding.repository.ts` | Queries de onboarding no banco |

### Modificar
| Arquivo | O que muda |
|---------|-----------|
| `apps/api/src/db/schema.ts` | Adicionar tabelas: therapists, mfa_secrets, onboarding_state |
| `apps/api/src/modules/auth/auth.service.ts` | Substituir mock por queries reais |
| `apps/api/src/modules/auth/auth.module.ts` | Registrar novos serviços |
| `apps/api/src/modules/auth/auth.controller.ts` | Adicionar POST /auth/register |
| `apps/api/src/app.module.ts` | Registrar OnboardingModule |
| `apps/api/.env.example` | Adicionar DATABASE_URL |
| `packages/contracts/src/auth.ts` | Adicionar tipos de register e onboarding |

---

## Task 1: Instalar dependências e configurar DATABASE_URL

**Files:**
- Modify: `apps/api/package.json`
- Modify: `apps/api/.env` (local only, nunca commitado)

- [ ] **Step 1.1: Instalar bcryptjs e @otplib/core**

```bash
cd apps/api
npm install bcryptjs @otplib/core @types/bcryptjs
```

Esperado: packages instalados sem erro.

- [ ] **Step 1.2: Verificar que DATABASE_URL está no .env apontando para Supabase**

O valor correto é a connection string do Supabase. Em `apps/api/.env`, adicionar:

```
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

> Pegar a string em: Supabase Dashboard → Settings → Database → Connection string → URI (Transaction pooler, porta 6543).

- [ ] **Step 1.3: Verificar que o cliente Drizzle conecta**

```bash
cd apps/api
npm run dev
```

Esperado: log sem erro de "DATABASE_URL not set". Se aparecer erro de conexão SSL, adicionar `?sslmode=require` no final da URL.

- [ ] **Step 1.4: Commit**

```bash
git add apps/api/package.json apps/api/package-lock.json
git commit -m "chore(api): add bcryptjs and otplib dependencies"
```

---

## Task 2: Schema base — tabelas de auth e onboarding

**Files:**
- Modify: `apps/api/src/db/schema.ts`

- [ ] **Step 2.1: Substituir schema.ts com as tabelas de auth**

```typescript
// apps/api/src/db/schema.ts
import { boolean, integer, pgTable, text, timestamp } from "drizzle-orm/pg-core";

export const tenants = pgTable("tenants", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  shortName: text("short_name").notNull(),
  status: text("status").notNull().default("active"),
  brazilOnlyProcessing: boolean("brazil_only_processing").notNull().default(true),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});

export const therapists = pgTable("therapists", {
  id: text("id").primaryKey(),
  tenantId: text("tenant_id").notNull().references(() => tenants.id),
  email: text("email").notNull().unique(),
  passwordHash: text("password_hash").notNull(),
  fullName: text("full_name").notNull(),
  role: text("role").notNull().default("owner"), // owner | member
  status: text("status").notNull().default("pending_onboarding"), // pending_onboarding | active | suspended
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});

export const mfaSecrets = pgTable("mfa_secrets", {
  id: text("id").primaryKey(),
  therapistId: text("therapist_id").notNull().references(() => therapists.id),
  secret: text("secret").notNull(), // base32 TOTP secret
  verified: boolean("verified").notNull().default(false),
  recoveryCodes: text("recovery_codes").notNull().default("[]"), // JSON array
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});

export const onboardingState = pgTable("onboarding_state", {
  id: text("id").primaryKey(),
  therapistId: text("therapist_id").notNull().references(() => therapists.id).unique(),
  completedSteps: text("completed_steps").notNull().default("[]"), // JSON array of step IDs
  practiceName: text("practice_name"),
  practiceCity: text("practice_city"),
  practiceState: text("practice_state"),
  firstPatientCreated: boolean("first_patient_created").notNull().default(false),
  completedAt: timestamp("completed_at", { withTimezone: true }),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});
```

- [ ] **Step 2.2: Verificar que o TypeScript compila o schema**

```bash
cd apps/api
npx tsc --noEmit
```

Esperado: sem erros de tipo.

- [ ] **Step 2.3: Commit**

```bash
git add apps/api/src/db/schema.ts
git commit -m "feat(db): add therapists, mfa_secrets and onboarding_state tables"
```

---

## Task 3: Gerar e rodar a migration

**Files:**
- Create: `apps/api/drizzle/` (pasta gerada pelo drizzle-kit)
- Modify: `apps/api/drizzle.config.ts`

- [ ] **Step 3.1: Verificar drizzle.config.ts**

```bash
cat apps/api/drizzle.config.ts
```

Deve conter `out: "./drizzle"` e `schema: "./src/db/schema.ts"`. Se não, corrigir:

```typescript
// apps/api/drizzle.config.ts
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  schema: "./src/db/schema.ts",
  out: "./drizzle",
  dialect: "postgresql",
  dbCredentials: {
    url: process.env.DATABASE_URL!
  }
});
```

- [ ] **Step 3.2: Gerar a migration**

```bash
cd apps/api
DATABASE_URL="<sua url>" npx drizzle-kit generate
```

Esperado: arquivo criado em `drizzle/0000_*.sql` com os CREATE TABLE.

- [ ] **Step 3.3: Aplicar a migration no banco**

```bash
cd apps/api
DATABASE_URL="<sua url>" npx drizzle-kit migrate
```

Esperado: "migrations applied successfully".

- [ ] **Step 3.4: Verificar no Supabase**

Abrir Supabase Dashboard → Table Editor. Confirmar que as tabelas `tenants`, `therapists`, `mfa_secrets`, `onboarding_state` existem.

- [ ] **Step 3.5: Commit**

```bash
git add apps/api/drizzle/ apps/api/drizzle.config.ts
git commit -m "feat(db): add initial migration for auth and onboarding tables"
```

---

## Task 4: PasswordService — hash e verificação com bcrypt

**Files:**
- Create: `apps/api/src/modules/auth/password.service.ts`

- [ ] **Step 4.1: Criar PasswordService**

```typescript
// apps/api/src/modules/auth/password.service.ts
import { Injectable } from "@nestjs/common";
import bcrypt from "bcryptjs";

const SALT_ROUNDS = 12;

@Injectable()
export class PasswordService {
  async hash(plaintext: string): Promise<string> {
    return bcrypt.hash(plaintext, SALT_ROUNDS);
  }

  async verify(plaintext: string, hash: string): Promise<boolean> {
    return bcrypt.compare(plaintext, hash);
  }
}
```

- [ ] **Step 4.2: Commit**

```bash
git add apps/api/src/modules/auth/password.service.ts
git commit -m "feat(auth): add PasswordService with bcrypt hash and verify"
```

---

## Task 5: TherapistRepository — queries de auth no banco

**Files:**
- Create: `apps/api/src/modules/auth/therapist.repository.ts`
- Modify: `apps/api/src/db/client.ts` (exportar tipo DrizzleClient)

- [ ] **Step 5.1: Exportar tipo do cliente Drizzle**

```typescript
// apps/api/src/db/client.ts
import { drizzle } from "drizzle-orm/postgres-js";
import postgres from "postgres";

import type { EnvService } from "@/common/config/env.service";

export type DrizzleClient = ReturnType<typeof drizzle>;

export function createDatabaseClient(env: EnvService): DrizzleClient | null {
  if (!env.values.DATABASE_URL) {
    return null;
  }

  const sql = postgres(env.values.DATABASE_URL, {
    max: 10,
    prepare: false
  });

  return drizzle(sql);
}
```

- [ ] **Step 5.2: Criar TherapistRepository**

```typescript
// apps/api/src/modules/auth/therapist.repository.ts
import { randomUUID } from "node:crypto";

import { Inject, Injectable } from "@nestjs/common";
import { eq } from "drizzle-orm";

import { DATABASE_CLIENT } from "@/common/tokens";
import { tenants, therapists } from "@/db/schema";
import type { DrizzleClient } from "@/db/client";

export type CreateTherapistInput = {
  email: string;
  passwordHash: string;
  fullName: string;
  practiceName: string;
};

@Injectable()
export class TherapistRepository {
  constructor(@Inject(DATABASE_CLIENT) private readonly db: DrizzleClient) {}

  async findByEmail(email: string) {
    const rows = await this.db
      .select()
      .from(therapists)
      .where(eq(therapists.email, email.toLowerCase()))
      .limit(1);

    return rows[0] ?? null;
  }

  async findById(id: string) {
    const rows = await this.db
      .select()
      .from(therapists)
      .where(eq(therapists.id, id))
      .limit(1);

    return rows[0] ?? null;
  }

  async create(input: CreateTherapistInput) {
    const tenantId = randomUUID();
    const therapistId = randomUUID();

    await this.db.insert(tenants).values({
      id: tenantId,
      name: input.practiceName,
      shortName: input.practiceName.slice(0, 30),
      status: "active"
    });

    const [therapist] = await this.db
      .insert(therapists)
      .values({
        id: therapistId,
        tenantId,
        email: input.email.toLowerCase(),
        passwordHash: input.passwordHash,
        fullName: input.fullName,
        role: "owner",
        status: "pending_onboarding"
      })
      .returning();

    return therapist;
  }
}
```

- [ ] **Step 5.3: Verificar token DATABASE_CLIENT**

```bash
grep -r "DATABASE_CLIENT" apps/api/src/common/ 2>/dev/null || echo "não existe"
```

Se não existir, criar:

```typescript
// apps/api/src/common/tokens.ts
export const DATABASE_CLIENT = Symbol("DATABASE_CLIENT");
```

- [ ] **Step 5.4: Commit**

```bash
git add apps/api/src/modules/auth/therapist.repository.ts apps/api/src/db/client.ts apps/api/src/common/tokens.ts
git commit -m "feat(auth): add TherapistRepository with create and findByEmail"
```

---

## Task 6: MfaService — TOTP real com @otplib/core

**Files:**
- Create: `apps/api/src/modules/auth/mfa.service.ts`
- Create: `apps/api/src/modules/auth/mfa.repository.ts`

- [ ] **Step 6.1: Criar MfaRepository**

```typescript
// apps/api/src/modules/auth/mfa.repository.ts
import { randomUUID } from "node:crypto";

import { Inject, Injectable } from "@nestjs/common";
import { eq } from "drizzle-orm";

import { DATABASE_CLIENT } from "@/common/tokens";
import { mfaSecrets } from "@/db/schema";
import type { DrizzleClient } from "@/db/client";

@Injectable()
export class MfaRepository {
  constructor(@Inject(DATABASE_CLIENT) private readonly db: DrizzleClient) {}

  async findByTherapistId(therapistId: string) {
    const rows = await this.db
      .select()
      .from(mfaSecrets)
      .where(eq(mfaSecrets.therapistId, therapistId))
      .limit(1);

    return rows[0] ?? null;
  }

  async create(therapistId: string, secret: string, recoveryCodes: string[]) {
    const [row] = await this.db
      .insert(mfaSecrets)
      .values({
        id: randomUUID(),
        therapistId,
        secret,
        verified: false,
        recoveryCodes: JSON.stringify(recoveryCodes)
      })
      .returning();

    return row;
  }

  async markVerified(therapistId: string) {
    await this.db
      .update(mfaSecrets)
      .set({ verified: true })
      .where(eq(mfaSecrets.therapistId, therapistId));
  }

  async consumeRecoveryCode(therapistId: string, code: string): Promise<boolean> {
    const row = await this.findByTherapistId(therapistId);
    if (!row) return false;

    const codes: string[] = JSON.parse(row.recoveryCodes);
    const index = codes.indexOf(code);
    if (index === -1) return false;

    codes.splice(index, 1);
    await this.db
      .update(mfaSecrets)
      .set({ recoveryCodes: JSON.stringify(codes) })
      .where(eq(mfaSecrets.therapistId, therapistId));

    return true;
  }
}
```

- [ ] **Step 6.2: Criar MfaService**

```typescript
// apps/api/src/modules/auth/mfa.service.ts
import { randomBytes } from "node:crypto";

import { Injectable, Inject } from "@nestjs/common";
import { authenticator } from "@otplib/core";
import { createDigits } from "@otplib/core";

import { MfaRepository } from "./mfa.repository";

const ISSUER = "Luma Manager";
const RECOVERY_CODE_COUNT = 8;

@Injectable()
export class MfaService {
  constructor(@Inject(MfaRepository) private readonly mfaRepository: MfaRepository) {}

  generateSecret(): string {
    return authenticator.generateSecret(20);
  }

  getOtpAuthUrl(email: string, secret: string): string {
    return authenticator.keyuri(email, ISSUER, secret);
  }

  verify(code: string, secret: string): boolean {
    return authenticator.verify({ token: code, secret });
  }

  generateRecoveryCodes(): string[] {
    return Array.from({ length: RECOVERY_CODE_COUNT }, () =>
      randomBytes(5).toString("hex").toUpperCase()
    );
  }

  async setupForTherapist(therapistId: string): Promise<{ secret: string; recoveryCodes: string[] }> {
    const secret = this.generateSecret();
    const recoveryCodes = this.generateRecoveryCodes();
    await this.mfaRepository.create(therapistId, secret, recoveryCodes);
    return { secret, recoveryCodes };
  }

  async verifyForTherapist(therapistId: string, code: string): Promise<boolean> {
    const row = await this.mfaRepository.findByTherapistId(therapistId);
    if (!row) return false;

    // Tentar TOTP primeiro
    if (this.verify(code, row.secret)) {
      if (!row.verified) {
        await this.mfaRepository.markVerified(therapistId);
      }
      return true;
    }

    // Tentar recovery code
    return this.mfaRepository.consumeRecoveryCode(therapistId, code.toUpperCase());
  }

  async isConfigured(therapistId: string): Promise<boolean> {
    const row = await this.mfaRepository.findByTherapistId(therapistId);
    return row?.verified ?? false;
  }
}
```

- [ ] **Step 6.3: Commit**

```bash
git add apps/api/src/modules/auth/mfa.service.ts apps/api/src/modules/auth/mfa.repository.ts
git commit -m "feat(auth): add MfaService with real TOTP via otplib and recovery codes"
```

---

## Task 7: AuthService real — substituir mock

**Files:**
- Modify: `apps/api/src/modules/auth/auth.service.ts`

- [ ] **Step 7.1: Reescrever AuthService**

```typescript
// apps/api/src/modules/auth/auth.service.ts
import { randomUUID } from "node:crypto";

import { Inject, Injectable, UnauthorizedException, ConflictException } from "@nestjs/common";
import type { AuthLoginRequest, AuthLoginResponse, AuthSession } from "@terapia/contracts";

import { MfaService } from "./mfa.service";
import { PasswordService } from "./password.service";
import { TherapistRepository } from "./therapist.repository";
import { AppSessionService } from "./app-session.service";

type PendingChallenge = {
  therapistId: string;
  email: string;
  expiresAt: number;
};

type RegisterInput = {
  email: string;
  password: string;
  fullName: string;
  practiceName: string;
};

@Injectable()
export class AuthService {
  private readonly pendingChallenges = new Map<string, PendingChallenge>();

  constructor(
    @Inject(AppSessionService) private readonly appSessionService: AppSessionService,
    @Inject(TherapistRepository) private readonly therapistRepository: TherapistRepository,
    @Inject(PasswordService) private readonly passwordService: PasswordService,
    @Inject(MfaService) private readonly mfaService: MfaService
  ) {}

  async register(input: RegisterInput): Promise<{ therapistId: string; mfaSecret: string; otpAuthUrl: string; recoveryCodes: string[] }> {
    const existing = await this.therapistRepository.findByEmail(input.email);
    if (existing) {
      throw new ConflictException("Este e-mail já está cadastrado.");
    }

    const passwordHash = await this.passwordService.hash(input.password);
    const therapist = await this.therapistRepository.create({
      email: input.email,
      passwordHash,
      fullName: input.fullName,
      practiceName: input.practiceName
    });

    const { secret, recoveryCodes } = await this.mfaService.setupForTherapist(therapist.id);
    const otpAuthUrl = this.mfaService.getOtpAuthUrl(input.email, secret);

    return { therapistId: therapist.id, mfaSecret: secret, otpAuthUrl, recoveryCodes };
  }

  async login(input: AuthLoginRequest): Promise<AuthLoginResponse> {
    const therapist = await this.therapistRepository.findByEmail(input.email);

    if (!therapist) {
      // Timing-safe: sempre verifica hash mesmo quando usuário não existe
      await this.passwordService.verify(input.password, "$2a$12$invalidsafehashplaceholder000000000000000000000");
      throw new UnauthorizedException("E-mail ou senha incorretos.");
    }

    const passwordValid = await this.passwordService.verify(input.password, therapist.passwordHash);
    if (!passwordValid) {
      throw new UnauthorizedException("E-mail ou senha incorretos.");
    }

    const challengeId = randomUUID();
    this.pendingChallenges.set(challengeId, {
      therapistId: therapist.id,
      email: therapist.email,
      expiresAt: Date.now() + 5 * 60 * 1000
    });

    return {
      challengeId,
      requiresMfa: true,
      mfaMethod: "totp",
      expiresInSeconds: 300,
      hint: "Use o aplicativo autenticador configurado na sua conta."
    };
  }

  async verifyMfa(input: { challengeId: string; code: string }): Promise<AuthSession> {
    const challenge = this.pendingChallenges.get(input.challengeId);

    if (!challenge || challenge.expiresAt < Date.now()) {
      throw new UnauthorizedException("Desafio de MFA expirado. Inicie o login novamente.");
    }

    const valid = await this.mfaService.verifyForTherapist(challenge.therapistId, input.code);
    if (!valid) {
      throw new UnauthorizedException("Código de MFA inválido.");
    }

    this.pendingChallenges.delete(input.challengeId);

    const therapist = await this.therapistRepository.findById(challenge.therapistId);
    if (!therapist) {
      throw new UnauthorizedException("Conta não encontrada.");
    }

    const expiresAt = new Date(Date.now() + 8 * 60 * 60 * 1000).toISOString();
    const session: Omit<AuthSession, "accessToken"> = {
      therapist: {
        id: therapist.id,
        email: therapist.email,
        fullName: therapist.fullName,
        tenantId: therapist.tenantId,
        role: therapist.role as AuthSession["therapist"]["role"]
      },
      accountStatus: therapist.status === "pending_onboarding" ? "pending_setup" : "ready_for_operations",
      capabilities: {
        audioTranscription: false,
        videoCall: false
      },
      mfaVerified: true,
      expiresAt
    };

    const accessToken = await this.appSessionService.sign(session);
    return { ...session, accessToken };
  }
}
```

- [ ] **Step 7.2: Adicionar POST /auth/register ao controller**

```typescript
// No auth.controller.ts, adicionar após o método login existente:

@Post("register")
async register(@Body() body: { email: string; password: string; fullName: string; practiceName: string }) {
  return this.authService.register(body);
}
```

- [ ] **Step 7.3: Commit**

```bash
git add apps/api/src/modules/auth/auth.service.ts apps/api/src/modules/auth/auth.controller.ts
git commit -m "feat(auth): replace mock with real DB auth, bcrypt passwords, real TOTP MFA"
```

---

## Task 8: Registrar novos serviços no AuthModule

**Files:**
- Modify: `apps/api/src/modules/auth/auth.module.ts`

- [ ] **Step 8.1: Ler o auth.module.ts atual**

```bash
cat apps/api/src/modules/auth/auth.module.ts
```

- [ ] **Step 8.2: Atualizar AuthModule para registrar todos os serviços**

```typescript
// apps/api/src/modules/auth/auth.module.ts
import { Module } from "@nestjs/common";

import { AppSessionService } from "./app-session.service";
import { AuthController } from "./auth.controller";
import { AuthService } from "./auth.service";
import { MfaRepository } from "./mfa.repository";
import { MfaService } from "./mfa.service";
import { PasswordService } from "./password.service";
import { TherapistRepository } from "./therapist.repository";

@Module({
  controllers: [AuthController],
  providers: [
    AppSessionService,
    AuthService,
    MfaRepository,
    MfaService,
    PasswordService,
    TherapistRepository
  ],
  exports: [AppSessionService, TherapistRepository]
})
export class AuthModule {}
```

- [ ] **Step 8.3: Verificar que o AppModule injeta DATABASE_CLIENT**

```bash
cat apps/api/src/app.module.ts | grep -A5 "DATABASE_CLIENT\|provide"
```

Se não encontrar, adicionar o provider no AppModule:

```typescript
// No providers do AppModule:
{
  provide: DATABASE_CLIENT,
  inject: [EnvService],
  useFactory: (env: EnvService) => createDatabaseClient(env)
}
```

- [ ] **Step 8.4: Commit**

```bash
git add apps/api/src/modules/auth/auth.module.ts apps/api/src/app.module.ts
git commit -m "feat(auth): wire up AuthModule with all real services"
```

---

## Task 9: OnboardingService — máquina de estado persistida

**Files:**
- Create: `apps/api/src/modules/onboarding/onboarding.repository.ts`
- Create: `apps/api/src/modules/onboarding/onboarding.service.ts`
- Create: `apps/api/src/modules/onboarding/onboarding.controller.ts`
- Create: `apps/api/src/modules/onboarding/onboarding.module.ts`

Os steps do onboarding (em ordem obrigatória):
1. `mfa_setup` — Configurar autenticador (feito durante registro)
2. `practice_info` — Nome e localização do consultório
3. `first_patient` — Criar o primeiro paciente

- [ ] **Step 9.1: Criar OnboardingRepository**

```typescript
// apps/api/src/modules/onboarding/onboarding.repository.ts
import { randomUUID } from "node:crypto";

import { Inject, Injectable } from "@nestjs/common";
import { eq } from "drizzle-orm";

import { DATABASE_CLIENT } from "@/common/tokens";
import { onboardingState } from "@/db/schema";
import type { DrizzleClient } from "@/db/client";

@Injectable()
export class OnboardingRepository {
  constructor(@Inject(DATABASE_CLIENT) private readonly db: DrizzleClient) {}

  async findByTherapistId(therapistId: string) {
    const rows = await this.db
      .select()
      .from(onboardingState)
      .where(eq(onboardingState.therapistId, therapistId))
      .limit(1);

    return rows[0] ?? null;
  }

  async create(therapistId: string) {
    const [row] = await this.db
      .insert(onboardingState)
      .values({
        id: randomUUID(),
        therapistId,
        completedSteps: JSON.stringify(["mfa_setup"])
      })
      .returning();

    return row;
  }

  async completeStep(therapistId: string, step: string) {
    const current = await this.findByTherapistId(therapistId);
    if (!current) return;

    const steps: string[] = JSON.parse(current.completedSteps);
    if (!steps.includes(step)) steps.push(step);

    await this.db
      .update(onboardingState)
      .set({ completedSteps: JSON.stringify(steps), updatedAt: new Date() })
      .where(eq(onboardingState.therapistId, therapistId));
  }

  async savePracticeInfo(therapistId: string, data: { practiceName: string; city: string; state: string }) {
    await this.db
      .update(onboardingState)
      .set({
        practiceName: data.practiceName,
        practiceCity: data.city,
        practiceState: data.state,
        updatedAt: new Date()
      })
      .where(eq(onboardingState.therapistId, therapistId));

    await this.completeStep(therapistId, "practice_info");
  }

  async markFirstPatientCreated(therapistId: string) {
    await this.db
      .update(onboardingState)
      .set({
        firstPatientCreated: true,
        completedAt: new Date(),
        updatedAt: new Date()
      })
      .where(eq(onboardingState.therapistId, therapistId));

    await this.completeStep(therapistId, "first_patient");
  }
}
```

- [ ] **Step 9.2: Criar OnboardingService**

```typescript
// apps/api/src/modules/onboarding/onboarding.service.ts
import { Inject, Injectable, NotFoundException } from "@nestjs/common";
import type { AuthSession } from "@terapia/contracts";

import { OnboardingRepository } from "./onboarding.repository";

export type OnboardingStatus = {
  completedSteps: string[];
  currentStep: string | null;
  isComplete: boolean;
  practiceName: string | null;
  practiceCity: string | null;
  practiceState: string | null;
  firstPatientCreated: boolean;
};

const ORDERED_STEPS = ["mfa_setup", "practice_info", "first_patient"] as const;

@Injectable()
export class OnboardingService {
  constructor(@Inject(OnboardingRepository) private readonly repo: OnboardingRepository) {}

  async getStatus(session: AuthSession): Promise<OnboardingStatus> {
    const state = await this.repo.findByTherapistId(session.therapist.id);
    if (!state) throw new NotFoundException("Estado de onboarding não encontrado.");

    const completed: string[] = JSON.parse(state.completedSteps);
    const nextStep = ORDERED_STEPS.find((s) => !completed.includes(s)) ?? null;

    return {
      completedSteps: completed,
      currentStep: nextStep,
      isComplete: nextStep === null,
      practiceName: state.practiceName ?? null,
      practiceCity: state.practiceCity ?? null,
      practiceState: state.practiceState ?? null,
      firstPatientCreated: state.firstPatientCreated
    };
  }

  async savePracticeInfo(session: AuthSession, data: { practiceName: string; city: string; state: string }) {
    await this.repo.savePracticeInfo(session.therapist.id, data);
    return this.getStatus(session);
  }

  async markFirstPatientCreated(therapistId: string) {
    await this.repo.markFirstPatientCreated(therapistId);
  }

  async initForTherapist(therapistId: string) {
    const existing = await this.repo.findByTherapistId(therapistId);
    if (!existing) {
      await this.repo.create(therapistId);
    }
  }
}
```

- [ ] **Step 9.3: Criar OnboardingController**

```typescript
// apps/api/src/modules/onboarding/onboarding.controller.ts
import { Body, Controller, Get, Inject, Post } from "@nestjs/common";
import type { AuthSession } from "@terapia/contracts";

import { Session } from "@/common/decorators/session.decorator";

import { OnboardingService } from "./onboarding.service";

@Controller("v1/onboarding")
export class OnboardingController {
  constructor(@Inject(OnboardingService) private readonly onboardingService: OnboardingService) {}

  @Get("status")
  async getStatus(@Session() session: AuthSession) {
    return this.onboardingService.getStatus(session);
  }

  @Post("practice-info")
  async savePracticeInfo(
    @Session() session: AuthSession,
    @Body() body: { practiceName: string; city: string; state: string }
  ) {
    return this.onboardingService.savePracticeInfo(session, body);
  }
}
```

- [ ] **Step 9.4: Criar OnboardingModule e registrar no AppModule**

```typescript
// apps/api/src/modules/onboarding/onboarding.module.ts
import { Module } from "@nestjs/common";

import { OnboardingController } from "./onboarding.controller";
import { OnboardingRepository } from "./onboarding.repository";
import { OnboardingService } from "./onboarding.service";

@Module({
  controllers: [OnboardingController],
  providers: [OnboardingRepository, OnboardingService],
  exports: [OnboardingService]
})
export class OnboardingModule {}
```

No `apps/api/src/app.module.ts`, adicionar `OnboardingModule` no array de imports.

- [ ] **Step 9.5: Commit**

```bash
git add apps/api/src/modules/onboarding/
git commit -m "feat(onboarding): add OnboardingService with step machine persisted in DB"
```

---

## Task 10: Integrar onboarding no fluxo de registro

**Files:**
- Modify: `apps/api/src/modules/auth/auth.service.ts`

- [ ] **Step 10.1: Inicializar onboarding durante register()**

No `AuthService.register()`, após criar o therapist, chamar `onboardingService.initForTherapist()`:

```typescript
// Adicionar ao construtor do AuthService:
@Inject(OnboardingService) private readonly onboardingService: OnboardingService

// Adicionar ao final do método register(), antes do return:
await this.onboardingService.initForTherapist(therapist.id);
```

- [ ] **Step 10.2: Commit**

```bash
git add apps/api/src/modules/auth/auth.service.ts
git commit -m "feat(auth): init onboarding state on therapist registration"
```

---

## Task 11: Cookies seguros no frontend

**Files:**
- Modify: `apps/web/app/api/session/route.ts`
- Modify: `apps/web/app/api/session/logout/route.ts`

- [ ] **Step 11.1: Ler os routes de sessão atuais**

```bash
cat apps/web/app/api/session/route.ts
cat apps/web/app/api/session/logout/route.ts
```

- [ ] **Step 11.2: Garantir cookies com flags de produção**

No handler que seta o cookie, garantir:

```typescript
cookies().set(SESSION_COOKIE_NAME, token, {
  httpOnly: true,
  secure: process.env.NODE_ENV === "production",
  sameSite: "lax",
  path: "/",
  maxAge: 8 * 60 * 60 // 8 horas, igual ao JWT
});
```

- [ ] **Step 11.3: Commit**

```bash
git add apps/web/app/api/session/
git commit -m "fix(session): enforce httpOnly, secure and sameSite on session cookie"
```

---

## Task 12: Remover vars de mock do EnvService

**Files:**
- Modify: `apps/api/src/common/config/env.service.ts`
- Modify: `apps/api/.env`

- [ ] **Step 12.1: Ler o EnvService atual**

```bash
cat apps/api/src/common/config/env.service.ts
```

- [ ] **Step 12.2: Remover MOCK_MFA_CODE e AUTH_PROVIDER do schema Zod**

Remover do schema de validação de env:
- `MOCK_MFA_CODE`
- `AUTH_PROVIDER` (se for só "mock" | "supabase")
- `MOCK_THERAPIST_EMAIL`

- [ ] **Step 12.3: Remover do .env.example**

```bash
# Remover do .env.example:
# AUTH_PROVIDER=mock
# MOCK_THERAPIST_EMAIL=...
# MOCK_MFA_CODE=123456
```

- [ ] **Step 12.4: Commit**

```bash
git add apps/api/src/common/config/env.service.ts apps/api/.env.example
git commit -m "chore(auth): remove mock auth env vars (MOCK_MFA_CODE, AUTH_PROVIDER)"
```

---

## Task 13: Smoke test manual do fluxo completo

- [ ] **Step 13.1: Iniciar API e Web**

```bash
# Terminal 1
cd apps/api && npm run dev

# Terminal 2
cd apps/web && npm run dev
```

- [ ] **Step 13.2: Testar registro via curl**

```bash
curl -X POST http://localhost:4000/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"teste@consultorio.com.br","password":"MinhaS3nh@Segura","fullName":"Dr. Teste","practiceName":"Consultório Teste"}'
```

Esperado: `{ therapistId, mfaSecret, otpAuthUrl, recoveryCodes }`.

- [ ] **Step 13.3: Configurar autenticador**

Copiar `otpAuthUrl` e decodificar com `qrcode` npm ou qualquer QR generator. Escanear com Google Authenticator ou Authy.

- [ ] **Step 13.4: Testar login**

```bash
curl -X POST http://localhost:4000/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"teste@consultorio.com.br","password":"MinhaS3nh@Segura"}'
```

Esperado: `{ challengeId, requiresMfa: true }`.

- [ ] **Step 13.5: Testar MFA com código real do autenticador**

```bash
curl -X POST http://localhost:4000/v1/auth/mfa/verify \
  -H "Content-Type: application/json" \
  -d '{"challengeId":"<id acima>","code":"<código do app>"}'
```

Esperado: sessão JWT válida com `mfaVerified: true`.

- [ ] **Step 13.6: Verificar onboarding no banco**

Supabase Dashboard → Table Editor → `onboarding_state`. Confirmar que existe linha com o therapistId criado.

- [ ] **Step 13.7: Commit final de fechamento**

```bash
git add .
git commit -m "feat: complete auth real - Plano 1 concluído"
git push origin main
```

---

## Checklist de conclusão do Plano 1

- [ ] Banco conectado e tabelas criadas
- [ ] Registro cria therapist + tenant + MFA secret + onboarding state no banco
- [ ] Login valida senha com bcrypt (sem mock)
- [ ] MFA valida TOTP real com janela de tempo (sem MOCK_MFA_CODE)
- [ ] Recovery codes funcionam e são consumidos
- [ ] Cookie de sessão tem httpOnly + secure em produção
- [ ] Vars de mock removidas do env
- [ ] Fluxo completo testado manualmente (register → login → mfa → sessão válida)

---

## Próximo: Plano 2 — Módulos Core Real

Após concluir este plano, o Plano 2 trata de:
- Schema para patients, appointments, clinical_records
- PatientsService → Drizzle (remover basePatients seed)
- AppointmentsService → Drizzle (remover appointmentSeed, relógio real)
- ClinicalRecordsService → Drizzle
- Dashboard puxando dados reais
