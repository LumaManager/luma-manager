[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace @terapia/contracts && npm run build --workspace @terapia/api"

[deploy]
startCommand = "node apps/api/dist/main.js"
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
